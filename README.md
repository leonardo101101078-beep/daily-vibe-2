# Daily-Vibe 2.0

PWA 待辦與健康管理：Next.js 14、Supabase、離線優先（IndexedDB）與雲端同步。

- **開發**：Node ≥ 20，`cp .env.local.example .env.local` 後填寫金鑰，再 `npm install` / `npm run dev`
- **部署**：見 [DEPLOY.md](./DEPLOY.md)（Supabase、**新 GitHub 倉庫**、Vercel）

產品顯示名稱與套件說明見 `package.json` 與 `lib/app-brand.ts`。
